# name: Build and deploy to GitHub Pages

# on:
#   push:
#     branches: [main]

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout 🛎️
#         uses: actions/checkout@v2.3.1

#       - name: Install and Build 🔧
#         run: |
#           npm ci
#           npm run build
#       - name: Deploy 🚀
#         uses: JamesIves/github-pages-deploy-action@4.1.0
#         with:
#           branch: gh-pages
#           folder: dist
name: Deploy GitHub Pages from src

on:
  push:
    branches:
      - master  # Или main, если у вас она основная

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Move files from src to root
      run: |
        mkdir -p docs
        cp -r src/* docs/

    - name: Setup Pages
      uses: actions/configure-pages@v3

    - name: Deploy to GitHub Pages
      uses: jamesives/github-pages-deploy-action@v4
      with:
        branch: gh-pages  # Публикация в отдельной ветке
        folder: docs  # Публикация из папки docs
